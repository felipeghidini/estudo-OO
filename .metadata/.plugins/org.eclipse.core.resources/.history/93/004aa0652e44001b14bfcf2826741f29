package test;

import static org.junit.Assert.*;

import org.junit.Test;

import entities.Veiculo;
import junit.framework.Assert;

public class VeiculoTest {
	
	@Test
	public void testAcelerar() throws Exception {
		Veiculo veiculo = new Veiculo("Fiat", "Palio", "DYA 5386", "Prata", 86000, 10000);
		int velocidade = 0;
		
		Assert.assertEquals(veiculo.acelerar(), veiculo.isLigado()actual);
		
//		if(veiculo.isLigado() == true) {
//			velocidade += 20;
//		}else {
//			System.out.println("Carro está desligado e não pode acelerar !");
//		}
	}

}
